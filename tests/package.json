{
  "name": "wp-kindness-hearts-tests",
  "version": "1.0.0",
  "type": "module",
  "description": "Playwright integration tests for the Kindness Hearts WordPress plugin",
  "scripts": {
    "test":        "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui":     "playwright test --ui",
    "test:report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0"
  }
}
