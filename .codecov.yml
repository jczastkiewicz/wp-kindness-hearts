coverage:
  status:
    project:
      default:
        target: 90
    patch:
      # Default: don't fail PRs on coverage for non-JS changes
      default:
        target: 0
      # Enforce a patch coverage target for the JS/TS app source only
      app/src/**:
        target: 90
